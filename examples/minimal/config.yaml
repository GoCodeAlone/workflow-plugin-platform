# Minimal config exercising workflow-plugin-platform.
# Schema-validate with: wfctl validate --skip-unknown-types examples/minimal/config.yaml
version: 1
modules:
  - name: platform-provider
    type: platform.provider
    config: {}
workflows:
  pipeline:
    steps:
      - name: example
        type: step.platform_template
        config: {}
